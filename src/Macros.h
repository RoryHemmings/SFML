#pragma once

#ifndef GAURD_MACROS_H
#define GAURD_MACROS_H

#define GET_TEXTURE(x) sgf::TextureManager::getInstance().getTexture(x)
#define TEXTURE_MANAGER sgf::TextureManager::getInstance()

#endif
